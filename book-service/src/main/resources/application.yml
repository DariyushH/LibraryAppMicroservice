server:
  port: 8089

spring:
  application:
    name: book-service
  cloud:
    consul:
      host: consul
      port: 8500
      discovery:
        enabled: true
    config:
      discovery:
        enabled: true
        service-id: config-server
  config:
    import: optional:configserver:config-server

